<?xml version="1.0" encoding="UTF-8"?>
<!--
  Kept here to mirror the Maven/Karate layout. The TypeScript Cucumber-JS stack
  does not use Logback directly; cucumber-js + WebdriverIO + Playwright write
  their own logs. This file is a placeholder so the resource tree matches the
  reference structure and CI/Maven-aware tooling can still parse it.
-->
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>target/test.log</file>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
